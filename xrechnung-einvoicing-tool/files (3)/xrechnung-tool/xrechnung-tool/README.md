# XRechnung / ZUGFeRD E-Invoicing Compliance Tool

A Spring Boot service implementing the core of the German B2B e-invoicing
mandate (EN 16931 / XRechnung), built from the accompanying [SRS](./SRS.md).

## What's actually implemented (and verified working)

- **XRechnung XML generation** (`generation/XRechnungGenerator.java`) — builds
  a UBL 2.1 Invoice XML with seller/buyer identification, line items, VAT
  category/rate per line, a real computed VAT breakdown (`TaxTotal`/
  `TaxSubtotal` grouped by category+rate), and VAT-inclusive totals. Uses
  only JDK-native XML APIs, no external library.
- **EN 16931 business-rule validation** (`validation/En16931Validator.java`)
  — a representative, honestly-scoped subset of the real rule set (see
  "What's simplified" below), distinguishing **syntax errors** from
  **business-rule violations** per the SRS's FR-8.
- **Full domain model, repository layer, service layer, and REST API**
  matching the SRS's data model and Section 6.1 API surface, including:
  - An `EInvoice` lifecycle (DRAFT → VALIDATED/REJECTED → SENT → ARCHIVED)
  - An `ArchiveEntryRepository` that **doesn't expose delete methods at
    all** (extends the bare `Repository` marker, not `JpaRepository`) —
    immutability enforced at the type level, not just by convention
  - Real email transmission via `JavaMailSender`
  - A compliance dashboard service (readiness rate, alerts)

## How this was actually verified

This project was built in a sandboxed environment **without access to Maven
Central** (confirmed: `repo.maven.apache.org` returns a network-level
`403 host_not_allowed`). That means the full Spring Boot application in this
repo has **not** been compiled or run end-to-end by me — I could not `mvn
compile` or `mvn test` it.

What I *could* do, and did:

1. Installed a JDK (`apt-get install openjdk-21-jdk-headless` — Ubuntu's
   package mirror was reachable even though Maven Central wasn't).
2. Copied out the dependency-free core (`domain` records, `XRechnungGenerator`,
   `En16931Validator` — none of which import Spring or JPA) and compiled
   them directly with `javac`, no build tool involved.
3. Wrote a standalone verification harness (not JUnit — JUnit's jar is also
   on Maven Central) exercising realistic invoice data: a valid multi-line
   invoice, a missing-mandatory-field case, a VAT-category/rate mismatch,
   an unrecognised currency, and a null/empty invoice.
4. Ran it. It caught **two real issues**: my first test assertion had the
   wrong arithmetic (not a code bug), and — genuinely — the generator was
   not computing an actual VAT amount; `TaxInclusiveAmount` was silently
   equal to the net total. I fixed the generator properly (grouped VAT
   breakdown by category+rate, real `TaxTotal` element) and re-verified:
   net 2000.00 → VAT 380.00 → gross 2380.00.
5. Also double-checked a "ß" mangling I saw in console output — confirmed
   via `file` and a UTF-8-aware read that it was this sandbox's POSIX/ASCII
   terminal locale, not a real encoding bug in the generated bytes.

The `src/test/java` directory contains proper **JUnit 5** tests (not the
standalone harness) mirroring — and extending — everything the harness
checked, ready to run with `mvn test` once you have normal internet access.
I additionally hand-verified the three extra assertions in those JUnit
files that weren't in the original harness (zero-rate standard category,
malformed VAT ID, negative quantity) against the real validator before
committing to them, the same way.

**In short: the core logic (generation + validation) is real, tested, and
had a genuine bug found and fixed. The Spring wiring (controllers, JPA
entities, service beans) is written to the same standard but is unverified
by compilation in this environment — build it locally and run `mvn test`
before trusting it further.**

## What's simplified (be upfront about this in interviews)

- **EN 16931 rule coverage**: `En16931Validator` implements a deliberately
  chosen subset (~15 rules) covering the categories the SRS calls out:
  missing identification, VAT category/rate correctness, currency, basic
  arithmetic. The official KoSIT Schematron rule set has 100+ rules. For
  production use, swap this for a real Schematron engine run against
  KoSIT's published rules (the **Mustangproject** library is the standard
  choice here) — this sandbox couldn't fetch it from Maven Central to
  integrate directly, so it's called out as a clear extension point instead
  of faked.
- **ZUGFeRD generation (FR-2)**: not implemented. A true PDF/A-3 with
  embedded XML needs a PDF library with PDF/A-3 attachment support
  (Mustangproject again, or Apache PDFBox). XRechnung (pure XML) is fully
  implemented and is, in practice, the lower-friction format for a B2B
  sender since it needs no PDF tooling at all.
- **Peppol transmission (FR-11)**: intentionally stubbed
  (`PeppolAccessPointClient` interface + a `PeppolConfig` bean that throws
  `UnsupportedOperationException`). Joining the Peppol network requires
  becoming or contracting a certified Access Point operator — an
  organizational/legal step, not something fakeable in a portfolio repo.
  The integration seam is there; a real client just needs to be dropped in.
- **Incoming document parsing (FR-15)**: `EInvoiceService.registerIncoming`
  accepts already-parsed `SourceInvoice` data — it does not itself parse an
  arbitrary uploaded XRechnung XML or ZUGFeRD PDF back into structured data.
  That's a reasonably substantial feature in its own right (XML deserialization
  + PDF/A-3 attachment extraction) that would be the natural next slice of work.

## Project layout

```
src/main/java/com/tsegabrehan/einvoicing/
  domain/        entities + the SourceInvoice input model
  generation/    XRechnungGenerator (pure Java, no deps)
  validation/    En16931Validator (pure Java, no deps)
  repository/    Spring Data interfaces (note ArchiveEntryRepository)
  service/       orchestration, transmission, archiving, dashboard
  api/           REST controller + DTOs
  config/        bean wiring for the pure-Java classes + Peppol stub
src/test/java/   JUnit 5 tests for generation + validation
```

## Running it (once you have normal internet access)

```bash
mvn spring-boot:run
```

Starts on `:8080` with an in-memory H2 database (see `application.yml`).
For PostgreSQL, run with `-Dspring-boot.run.profiles=postgres` and set the
`DB_HOST`/`DB_NAME`/`DB_USERNAME`/`DB_PASSWORD` environment variables.

```bash
mvn test
```

Runs the JUnit 5 suite.

### Example: generate an invoice

```bash
curl -X POST localhost:8080/api/v1/einvoices \
  -H "Content-Type: application/json" \
  -d '{
    "invoiceNumber": "INV-2026-0001",
    "issueDate": "2026-07-15",
    "currencyCode": "EUR",
    "seller": {"name":"Tsegabrehan Software GmbH","vatId":"DE123456789","countryCode":"DE"},
    "buyer": {"name":"Musterkunde AG","vatId":"DE987654321","countryCode":"DE"},
    "lines": [{"lineId":"1","description":"Consulting","quantity":10,"unitPrice":150.00,"vatCategoryCode":"S","vatRatePercent":19.00}]
  }'
```

## Relationship to the rest of the portfolio

Designed to sit behind the **Financial & Inventory Management Platform**
project's `SalesOrder`/`Invoice` records — that system produces the business
data, this service turns it into a compliant e-invoice.
